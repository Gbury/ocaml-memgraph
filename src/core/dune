
(documentation
  (package memgraph)
)

(library
  (name         memgraph)
  (public_name  memgraph)
  (libraries    unix)
  (modules      Repr Dot)
  (preprocess (action (run %{bin:cppo} -V OCAML:%{ocaml_version} %{input-file})))
)

